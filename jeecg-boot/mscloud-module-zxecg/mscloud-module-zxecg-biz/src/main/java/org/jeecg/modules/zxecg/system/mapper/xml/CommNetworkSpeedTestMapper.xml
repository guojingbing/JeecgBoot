<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.zxecg.system.mapper.CommNetworkSpeedTestMapper">
    <select id="loadListPaging" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select * from (
        SELECT
        w.id,
        w.download_speed downloadSpeed,
        w.resp_speed respSpeed,
        w.resp_shake respShake,
        w.remark,
        DATE_FORMAT(
        w.test_time,
        '%Y-%m-%d %H:%i:%s'
        ) testTime,
        w.test_user_id testUserId,
        csu.user_name userName
        FROM
        comm_network_speed_test w
        LEFT JOIN comm_system_user csu ON w.test_user_id = csu.user_id
        ) temp where 1=1
        <foreach collection="likeMap" item="tm" index="key">
            <if test='tm != null and tm.toString().trim() != ""'>
                and ${key} like CONCAT('%',#{tm,jdbcType=VARCHAR}, '%')
            </if>
        </foreach>
        <choose>
            <when test="column!= null and column!='' ">
                order by ${column} ${order}
            </when>
            <otherwise>
                order by testTime desc
            </otherwise>
        </choose>
    </select>


</mapper>